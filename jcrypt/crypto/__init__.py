"""
JCRYPT crypto package.

Contains all cryptographic, encoding, and hashing logic used by the
Flask application. Nothing in this package touches the filesystem,
a database, or the network — every function is a pure transformation
of the input it is given.
"""
